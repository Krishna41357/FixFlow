import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Pipeline Autopsy - Data Lineage & Investigation Platform",
  description: "Intelligent pipeline failure analysis and data lineage visualization powered by AI",
  keywords: "data pipeline, failure analysis, lineage, data governance, investigation",
  authors: [{ name: "Pipeline Autopsy Team" }],
  viewport: "width=device-width, initial-scale=1",
  themeColor: "#dc2626",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
